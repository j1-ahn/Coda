# Coda Studio Backend — 환경 변수 예시
# 복사 후 .env 로 이름 변경

# 허용할 CORS Origin (콤마 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 서버 포트
PORT=8000

# Whisper 기본 모델 (large-v3 권장, VRAM 부족 시 medium 사용)
WHISPER_MODEL=large-v3
